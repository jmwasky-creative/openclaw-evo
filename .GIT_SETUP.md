# Git 进化管理初始化说明

**仓库**: https://github.com/jmwasky-creative/openclaw-evo
**本地路径**: /root/.openclaw/workspace
**状态**: 本地已初始化，待推送认证

---

## ✅ 已完成

1. **本地仓库初始化**
   - git init
   - 配置用户信息 (CC AI Assistant <964692548@qq.com>)

2. **首次提交**
   - Commit: 21a6710
   - 14 个文件，1171 行新增
   - 包含: PCEC、能力树、记忆系统

3. **.gitignore 配置**
   - 排除敏感信息 (openclaw.json, *.key, *.token)
   - 排除临时文件 (logs, temp)
   - 排除会话数据 (sessions/, *.jsonl)
   - **保留进化文件** (.PCEC.md, .CAPABILITY_TREE.md, memory/)

---

## 🔑 待配置：GitHub 认证

### 方法 1: 使用 Personal Access Token

**步骤**:
1. 访问 https://github.com/settings/tokens
2. 生成新的 PAT (Classic)
3. 授予 `repo` 权限
4. 运行:
```bash
export GITHUB_TOKEN="your_token_here"
cd /root/.openclaw/workspace
git remote set-url origin https://jmwasky-creative:${GITHUB_TOKEN}@github.com/jmwasky-creative/openclaw-evo.git
git push -u origin main
```

### 方法 2: 使用 SSH Key

**步骤**:
1. 生成 SSH key:
```bash
ssh-keygen -t ed25519 -C "964692548@qq.com"
```

2. 添加到 GitHub:
   - 复制 `~/.ssh/id_ed25519.pub` 内容
   - 访问 https://github.com/settings/keys
   - 添加新 SSH key

3. 更改远程 URL:
```bash
cd /root/.openclaw/workspace
git remote set-url origin git@github.com:jmwasky-creative/openclaw-evo.git
git push -u origin main
```

---

## 📊 进化管理工作流

### 日常进化提交

```bash
cd /root/.openclaw/workspace

# 1. 添加变更
git add .PCEC.md .CAPABILITY_TREE.md memory/

# 2. 提交（使用进化类型）
git commit -m "evolution: [类型] 简短描述

[能力变化]
- 新增/修改/删除: 具体内容
- 影响范围: XX分支
- 验证状态: ✅/⚠️/❌

[反进化检查]
- 稳定性: 通过/待验证
- 可解释性: 通过/待验证
- 可复用性: 通过/待验证
- 回滚方案: 有/无"

# 3. 推送
git push
```

### 提交类型规范

- `evolution`: 新能力/能力改进
- `refactor`: 能力重构/合并
- `prune`: 能力修剪
- `docs`: 文档更新
- `rollback`: 回滚到稳定版本

### 分支策略

- `main`: 稳定进化版本
- `experiment/*`: 实验性能力
- `rollback/*`: 回滚快照

---

## 🔄 版本标签

**重要进化打标签**:
```bash
git tag -a v1.0.0 -m "Cycle X 完整进化"
git push origin v1.0.0
```

**回滚标签**:
```bash
git tag -a rollback-v1.0.0 -m "回滚点: 原因"
git push origin rollback-v1.0.0
```

---

## 📈 进化追踪

**查看进化历史**:
```bash
git log --oneline --graph
```

**查看能力变化**:
```bash
git log -p .CAPABILITY_TREE.md
```

**回滚到稳定版本**:
```bash
git checkout <commit-hash>
```

---

## ⏭️ 下一步

1. 配置 GitHub 认证 (选择上述方法之一)
2. 推送首次提交到远程
3. 后续每个 PCEC 周期提交一次进化
4. 重要里程碑打标签
